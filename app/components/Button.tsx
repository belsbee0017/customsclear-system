"use client"; import React, { useState } from "react"; type ButtonProps = { children: React.ReactNode; onClick?: () => void; variant?: "primary" | "outline"; disabled?: boolean; type?: "button" | "submit"; }; export default function Button({ children, onClick, variant = "primary", disabled = false, type = "button" }: ButtonProps) { const [hovered, setHovered] = useState(false); const [pressed, setPressed] = useState(false); const baseStyle: React.CSSProperties = { padding: "8px 14px", borderRadius: "6px", fontSize: "14px", fontWeight: "bold", cursor: disabled ? "not-allowed" : "pointer", transition: "all 0.15s ease", outline: "none", border: "none", transform: pressed ? "scale(0.96)" : "scale(1)", boxShadow: hovered && !disabled ? "0 4px 10px rgba(0,0,0,0.15)" : "none", opacity: disabled ? 0.6 : 1 }; const variants: Record<string, React.CSSProperties> = { primary: { backgroundColor: "#8aa8c2", color: "#141414" }, outline: { backgroundColor: "#ffffff", border: "1px solid #b00020", color: "#b00020" } }; return ( <button type={type} disabled={disabled} onClick={onClick} style={{ ...baseStyle, ...variants[variant] }} onMouseEnter={() => setHovered(true)} onMouseLeave={() => { setHovered(false); setPressed(false); }} onMouseDown={() => setPressed(true)} onMouseUp={() => setPressed(false)} > {children} </button> ); }